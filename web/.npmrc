# Keep `npm install` output calm during setup. The audit/funding notices are
# advisory and the only findings are build-time transitive deps (e.g. postcss
# bundled by Next), not exploitable for a local static-export tool. Run
# `npm audit` manually any time to see them.
audit=false
fund=false
