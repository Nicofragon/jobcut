import { Nav } from "web";

// The console's top navigation bar: diamond logo + wordmark, the primary
// section links (Today active by default), and the theme toggle. Full-bleed.
export function Default() {
  return <Nav />;
}
